# LLM Provider Key (Optional, agent falls back to rule-based planner if not provided)
# For Gemini (Recommended):
GEMINI_API_KEY=your_gemini_api_key_here

# For OpenAI (Alternative):
# OPENAI_API_KEY=your_openai_api_key_here

# Mem0 Platform API Key (Optional, agent falls back to local memory if not provided)
MEM0_API_KEY=your_mem0_api_key_here
